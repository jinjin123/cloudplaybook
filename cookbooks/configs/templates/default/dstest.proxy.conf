server {
  listen       80;
  server_name   172.16.104.96;
  #charset koi8-r;
  #access_log  logs/host.access.log  main;
  location /request_uri {
    #$request_uri;
    add_header Content-Type text/plain;
    return 200 $host;
  }
  location /crm {
    rewrite /crm(.*) /$1  break;
    proxy_redirect off;
    proxy_buffering off;
    proxy_pass      http://kungfudingcan_crm;
    # proxy_cache      cachehtml;
    # proxy_cache_valid      200  1d;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header   Host $http_host;
    #proxy_set_header   Host $host:8086;
    proxy_set_header   Referer $http_referer;
    proxy_set_header   Cookie $http_cookie;
    proxy_http_version 1.1;
    chunked_transfer_encoding      Off;
    # proxy_cache_use_stale  error timeout invalid_header updating http_500 http_502 http_503 http_504;
    proxy_connect_timeout       300;
    proxy_send_timeout          300;
    proxy_read_timeout          300;
    send_timeout                300;
  }
  location /de {
    proxy_redirect off;
    proxy_buffering off;
    proxy_pass      http://kungfudingcan_de;
    # proxy_cache      cachehtml;
    # proxy_cache_valid      200  1d;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header   Host $http_host;
    proxy_set_header   Referer $http_referer;
    proxy_set_header   Cookie $http_cookie;
    proxy_http_version 1.1;
    chunked_transfer_encoding      Off;
    # proxy_cache_use_stale  error timeout invalid_header updating http_500 http_502 http_503 http_504;
    proxy_connect_timeout       300;
    proxy_send_timeout          300;
    proxy_read_timeout          300;
    send_timeout          300;
  }
  location /pmt {
    proxy_redirect off;
    proxy_buffering off;
    proxy_pass      http://kungfudingcan_pmt;
    # proxy_cache      cachehtml;
    # proxy_cache_valid      200  1d;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header   Host $http_host;
    proxy_set_header   Referer $http_referer;
    proxy_set_header   Cookie $http_cookie;
    proxy_http_version 1.1;
    chunked_transfer_encoding      Off;
    # proxy_cache_use_stale  error timeout invalid_header updating http_500 http_502 http_503 http_504;
    proxy_connect_timeout       300;
    proxy_send_timeout          300;
    proxy_read_timeout          300;
    send_timeout          300;
  }
  location /mkt {
    proxy_redirect off;
    proxy_buffering off;
    proxy_pass      http://kungfudingcan_mkt;
    # proxy_cache      cachehtml;
    # proxy_cache_valid      200  1d;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header   Host $http_host;
    proxy_set_header   Referer $http_referer;
    proxy_set_header   Cookie $http_cookie;
    proxy_http_version 1.1;
    chunked_transfer_encoding      Off;
    # proxy_cache_use_stale  error timeout invalid_header updating http_500 http_502 http_503 http_504;
    proxy_connect_timeout       300;
    proxy_send_timeout          300;
    proxy_read_timeout          300;
    send_timeout          300;
  }
  location /erp {
    proxy_redirect off;
    proxy_buffering off;
    proxy_pass      http://kungfudingcan_erp;
    # proxy_cache      cachehtml;
    # proxy_cache_valid      200  1d;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header   Host $http_host;
    proxy_set_header   Referer $http_referer;
    proxy_set_header   Cookie $http_cookie;
    proxy_http_version 1.1;
    chunked_transfer_encoding      Off;
    # proxy_cache_use_stale  error timeout invalid_header updating http_500 http_502 http_503 http_504;
    proxy_connect_timeout       300;
    proxy_send_timeout          300;
    proxy_read_timeout          300;
    send_timeout          300;
  }
  location /cc {
    proxy_redirect off;
    proxy_buffering off;
    proxy_pass      http://kungfudingcan_cc;
    # proxy_cache      cachehtml;
    # proxy_cache_valid      200  1d;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header   Host $http_host;
    proxy_set_header   Referer $http_referer;
    proxy_set_header   Cookie $http_cookie;
    proxy_http_version 1.1;
    chunked_transfer_encoding      Off;
    # proxy_cache_use_stale  error timeout invalid_header updating http_500 http_502 http_503 http_504;
    proxy_connect_timeout       300;
    proxy_send_timeout          300;
    proxy_read_timeout          300;
    send_timeout          300;
  }
}
