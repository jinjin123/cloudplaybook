user  nginx;
worker_processes  1;

error_log  /var/log/nginx/error.log warn;
pid      /var/run/nginx.pid;


events {
  worker_connections  1024;
}


http {
  include     /etc/nginx/mime.types;
  default_type  application/octet-stream;

  client_max_body_size 900M;

  log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

  access_log  /var/log/nginx/access.log  main;

  sendfile      on;
  #tcp_nopush   on;

  keepalive_timeout  65;

  #gzip  on;

  include /etc/nginx/conf.d/*.conf;


  server {
    listen       8086;
    server_name   _;
    #charset koi8-r;
    #access_log  logs/host.access.log  main;
    location /request_uri {
      #$request_uri;
      add_header Content-Type text/plain;
      return 200 $host;
    }
    location /crm {
      proxy_redirect off;
      proxy_buffering off;
      proxy_pass      http://zkf_crmtc;
      # proxy_cache      cachehtml;
      # proxy_cache_valid      200  1d;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header   Host $http_host;
      #proxy_set_header   Host $host:8086;
      proxy_set_header   Referer $http_referer;
      proxy_set_header   Cookie $http_cookie;
      proxy_http_version 1.1;
      chunked_transfer_encoding      Off;
      # proxy_cache_use_stale  error timeout invalid_header updating http_500 http_502 http_503 http_504;
      proxy_connect_timeout       300;
      proxy_send_timeout          300;
      proxy_read_timeout          300;
      send_timeout                300;
    }
    location /zkf-crm {
      proxy_redirect off;
      proxy_buffering off;
      proxy_pass      http://zkf_crm;
      # proxy_cache      cachehtml;
      # proxy_cache_valid      200  1d;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header   Host $http_host;
      #proxy_set_header   Host $host:8086;
      proxy_set_header   Referer $http_referer;
      proxy_set_header   Cookie $http_cookie;
      proxy_http_version 1.1;
      chunked_transfer_encoding      Off;
      # proxy_cache_use_stale  error timeout invalid_header updating http_500 http_502 http_503 http_504;
      proxy_connect_timeout       300;
      proxy_send_timeout          300;
      proxy_read_timeout          300;
      send_timeout                300;
    }
    location /de {
      proxy_redirect off;
      proxy_buffering off;
      proxy_pass      http://zkf_de;
      # proxy_cache      cachehtml;
      # proxy_cache_valid      200  1d;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header   Host $http_host;
      proxy_set_header   Referer $http_referer;
      proxy_set_header   Cookie $http_cookie;
      proxy_http_version 1.1;
      chunked_transfer_encoding      Off;
      # proxy_cache_use_stale  error timeout invalid_header updating http_500 http_502 http_503 http_504;
      proxy_connect_timeout       300;
      proxy_send_timeout          300;
      proxy_read_timeout          300;
      send_timeout          300;
    }
    location /zkf-payment {
      proxy_redirect off;
      proxy_buffering off;
      proxy_pass      http://zkf_pmt;
      # proxy_cache      cachehtml;
      # proxy_cache_valid      200  1d;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header   Host $http_host;
      proxy_set_header   Referer $http_referer;
      proxy_set_header   Cookie $http_cookie;
      proxy_http_version 1.1;
      chunked_transfer_encoding      Off;
      # proxy_cache_use_stale  error timeout invalid_header updating http_500 http_502 http_503 http_504;
      proxy_connect_timeout       300;
      proxy_send_timeout          300;
      proxy_read_timeout          300;
      send_timeout          300;
    }
    location /zkf-marketing {
      proxy_redirect off;
      proxy_buffering off;
      proxy_pass      http://zkf_mkt;
      # proxy_cache      cachehtml;
      # proxy_cache_valid      200  1d;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header   Host $http_host;
      proxy_set_header   Referer $http_referer;
      proxy_set_header   Cookie $http_cookie;
      proxy_http_version 1.1;
      chunked_transfer_encoding      Off;
      # proxy_cache_use_stale  error timeout invalid_header updating http_500 http_502 http_503 http_504;
      proxy_connect_timeout       300;
      proxy_send_timeout          300;
      proxy_read_timeout          300;
      send_timeout          300;
    }
  }
}
